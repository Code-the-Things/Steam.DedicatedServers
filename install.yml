---
  - name: Install Steam Dedicated Server
    hosts: all
    gather_facts: true
    become: true
    vars:
      serverType: "{{ morpheus['instance']['layoutName'] }}"
  
    tasks:
      - include_role:
          name: steam
      - include_role:
          name: l4d2
        when: serverType == "L4D2"