---
  - name: Start Steam Server
    hosts: all
    gather_facts: true
    become: true
    become_user: steam

    tasks:
      - name: Start Steam Server
        shell:
          cmd: |
            script /dev/null
            screen -S steamserver bash -c "bash /steam/start.sh"